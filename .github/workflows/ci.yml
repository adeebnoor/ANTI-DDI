name: validate

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install package
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .

      - name: Re-derive v2 published quantities
        run: python validate.py --verbose

      - name: Rebuild v3 release tables with conservative semantics
        run: |
          python build_v3.py
          python semantic_harden_v3.py

      - name: Validate v3 semantic and release invariants
        run: python validate_v3.py

      - name: Validate corrective ATC5 structural-bias outputs
        run: python analysis/validate_degree_bias_outputs.py

      - name: Ensure superseded experiment is marked withdrawn
        run: |
          grep -q "withdrawn" VALIDATION_NOTICE_20260820.md
          grep -q "not valid as evidence" paper5/PAPER5_RIDI_VALIDATION.md

      - name: Persist hardened v3 tables on semantic release branch
        if: github.event_name == 'push' && github.ref == 'refs/heads/release/antiddi-v3.0.1-semantics'
        run: |
          if git diff --quiet -- data/antiddi_v3_dataset.csv data/antiddi_v3_benchmark.csv; then
            echo "Hardened tables already persisted."
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add data/antiddi_v3_dataset.csv data/antiddi_v3_benchmark.csv
            git commit -m "Persist conservative v3.0.1 candidate semantics"
            git push
          fi
